apiVersion: v1
kind: Service
metadata:
  name: {{ include "autokube.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "autokube.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "autokube.selectorLabels" . | nindent 4 }}
